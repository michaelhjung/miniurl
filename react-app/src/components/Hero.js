import React from "react";
// import escapeHatchLogo from "../assets/escape-hatch-logo.png";

const Hero = () => {
  return (
    <section className="hero-container">
      {/* <img id="hero-logo" src={escapeHatchLogo} alt="escape hatch logo" /> */}
    </section>
  );
};

export default Hero;
